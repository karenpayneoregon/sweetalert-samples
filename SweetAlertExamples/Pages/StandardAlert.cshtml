@page
@model SweetAlertExamples.Pages.StandardAlertModel
@{
}

@*
    if you have multiple pop-ups where there is a mixture of blurred and no blurred use the following not to blur
    
    Swal.mixin({
            backdrop: false,

*@
<style>
    body.swal2-shown > [aria-hidden="true"] {
        transition: 0.1s filter;
        filter: blur(10px);
    }

</style>

<div class="container">
    
    <div class="d-flex justify-content-center text-center w-75 position-absolute top-50 start-50 translate-middle">
        <p class="card-text fs-4">Provides several code samples using <a href="https://sweetalert2.github.io" target="_blank">sweet alert</a></p>
    </div>
    
    <form method="post" id="form1" class="mt-5">

        <div class="d-grid gap-2 col-6 mx-auto">

            <div class="row">
                <button type="button"
                        id="showAlert1"
                        class="btn btn-primary">
                    Basic animated
                </button>
            </div>
            
            <div class="row">
                <button type="button"
                        id="showAlert2"
                        class="btn btn-primary">
                    Basic not animated
                </button>
            </div>
            
            <div class="row">
                <button type="button"
                        id="showAlert3"
                        class="btn btn-primary">
                    Modal
                </button>
            </div>

            <div class="row">
                <button type="button"
                        id="showAlert4"
                        class="btn btn-primary">
                    Blurred backdrop 1
                </button>
            </div>
            
            <div class="row">
                <button type="button"
                        id="showAlert5"
                        class="btn btn-primary">
                    Blurred backdrop 2
                </button>
            </div>
        </div>

    </form>
</div>

<script>
    document.getElementById('showAlert1').addEventListener("click", function () {
        Swal.fire(
            'Good job!',
            'You clicked the button!',
            'success'
        );
    });

    document.getElementById('showAlert2').addEventListener("click", function () {

        const swalWithBootstrapButtons = Swal.mixin({
            backdrop: false,
            customClass: {
                confirmButton: 'btn btn-primary'
            },
            buttonsStyling: false
        });
        swalWithBootstrapButtons.fire({
            title: 'No Icon',
            html: "<div class = 'text-success fw-bold border border-success bg-light'>All done</div>",
            allowOutsideClick: false,
            allowEscapeKey: false,
            reverseButtons: true
        });

    });

    document.getElementById('showAlert3').addEventListener("click", function () {
        const swalWithBootstrapButtons = Swal.mixin({
            backdrop: false,
            customClass: {
                confirmButton: 'btn btn-danger'
            },
            buttonsStyling: false
        });

        swalWithBootstrapButtons.fire({
            title: 'Something went wrong',
            html: "<strong>Call the help desk</strong>",
            icon: 'error',
            allowOutsideClick: false,
            allowEscapeKey: false,
            reverseButtons: true
        });
    });

    // see styling at top of page
    document.getElementById('showAlert4').addEventListener("click", function () {
        Swal.fire({ title: 'We control what you can see!!!' });
    });

    document.getElementById('showAlert5').addEventListener("click", function () {

        Swal.fire(
                'Good job!',
                'You clicked the button!'
            );
            $(".swal2-modal").css('background-color', 'aliceblue');
    });
  
</script>